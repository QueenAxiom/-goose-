# Decisions Log — Axiom Enterprises

Referenced by `operations-manager.md` and `recorder.md` (global agent configs) as the locked source of truth for past decisions. Entries here are **locked**: once a row is added, it is not silently reprioritized around, reversed, or re-debated — only Alan can supersede a decision, and doing so should add a new row noting what changed and why, rather than editing the old one.

| Decision | Choice | Date |
|---|---|---|
| Runway API integration timing | Deferred — not until 2026-07-29 at the earliest (video automation not a priority yet) | 2026-07-24 |
| Messaging/telephony provider | RingCentral (existing paid account) instead of Twilio — no need to pay for/provision a second phone number when RingCentral's API covers SMS/voice | 2026-07-24 |
| RingCentral subscription workflow alert channel | Gmail draft (matches existing `content-log.md`/`prospecting-log.md` automation-notification pattern) over Slack or self-SMS | 2026-07-24 |
| YouTube scheduled auto-upload — scope vs. Runway deferral | Built now, not deferred. The 2026-07-24 Runway deferral is about AI video *generation*; this workflow only *publishes* videos Alan already created — different scope. Confirmed with Alan before building. | 2026-07-26 |
| YouTube scheduled auto-upload — default privacy status | `public` (true zero-touch publish), unlike every other content automation in this repo which drafts and waits for Alan to post manually | 2026-07-26 |
| LinkedIn Company Page scheduled auto-post — build status | Deliberately NOT imported into the live N8N instance and no working credential created. Verified live on Alan's LinkedIn Developer app that Company Page posting (`w_organization_social` / Community Management API) is blocked: the app already has "Share on LinkedIn" (personal-only, `w_member_social`) added, and LinkedIn will not let a second product be requested on the same app — a brand-new, single-product app plus a Page-verification/app-review process is required. Importable JSON + design doc exist at `automation/linkedin-scheduled-post.json` / `docs/linkedin-scheduled-post-workflow.md` for whenever access is unblocked. | 2026-07-27 |
| Threads scheduled auto-post — build status | BLOCKED. Verified on the same Meta app that the Threads API use case is added but not fully customized (no green checkmark on the app's own Dashboard checklist, unlike Pages API); `threads_basic` is "Ready for testing" but `threads_content_publish` (the actual posting permission) is not added. Threads runs on its own separate App ID (`1047046641402198`) and App Secret, distinct from the Facebook App ID/Secret already supplied — the secret was not retrieved. n8n has no dedicated Threads node (confirmed live in the node picker), so any build would need the generic HTTP Request node against `graph.threads.net`. Nothing was created or connected live in n8n. Importable JSON + design doc exist at `automation/threads-scheduled-post.json` / `docs/threads-scheduled-post-workflow.md` for whenever access is unblocked. | 2026-07-27 |
| Facebook/Threads scheduled auto-post — Guardian bypass | OPEN QUESTION, not decided: whether Facebook Page posts and Threads posts should bypass Guardian's Mandatory Post Elements checklist (`docs/platform-compliance.md`) for full zero-touch publish like YouTube, or still route through Guardian review before being marked ready to post. Same unresolved question first flagged on the LinkedIn build (2026-07-27) — still open across all three social builds. | 2026-07-27 |
| Facebook Page scheduled auto-post — build status | MOSTLY VIABLE, connected live but not fully activated. `automation/facebook-scheduled-post.json` is imported into the live N8N instance as its own workflow ("Facebook Scheduled Post"), with every node's credential auto-matched to existing account-level credentials (Facebook Graph account / Google Sheets account / Gmail account). `pages_manage_posts` and `pages_read_engagement` were added in the Meta console and are "Ready for testing". Alan completed the OAuth "Connect" login himself in n8n. Executing "Get Page Access Token" live showed the connected account manages exactly one Facebook Page: **Everythinginternet.ca**, Page ID `1096244530243584` — not a separate "Axiom Enterprises" Page as originally assumed. That ID is hardcoded into the "Extract Target Page Token" node in both `automation/facebook-scheduled-post.json` and the live n8n workflow. Only remaining blocker: creating the "Posts" Google Sheet and supplying its ID. See `docs/facebook-scheduled-post-workflow.md`'s 2026-07-28 follow-up pass. | 2026-07-28 |
| Facebook/Threads scheduled auto-post — Guardian bypass RESOLVED | Supersedes the 2026-07-27 open question. Alan decided: no zero-touch bypass. Guardian reviews the drafted post first, then it's sent to Alan's email for his approval before anything posts — same pattern as every other content automation in this repo (e.g. Pocket AI recorder posts), not the YouTube-style full auto-publish. This reverses the "full zero-touch auto-publish" design basis stated at the top of `docs/facebook-scheduled-post-workflow.md` — that workflow's direct-to-Facebook-API posting nodes need to be replaced with a Guardian-check + Gmail-draft-for-approval step. | 2026-08-02 |
| Website rebuild — bilingual EN/FR requirement | LOCKED, not optional. Quebec's Bill 96 (Charter of the French Language) applies extraterritorially: any company selling to or marketing toward Quebec consumers online must offer a full French version of the site (homepage, product/service descriptions, pricing, policies) with French not subordinate to English (not smaller, not harder to find, not an afterthought). None of Axiom's three sites geo-block Quebec, so this is a live compliance exposure, not a style preference — Quebec's language office has been actively fining companies over English-only pages ($3,000–$30,000 per violation). All three site rebuilds must ship full EN/FR parity with a language selector at the top of the page that swaps the entire site on selection. This is separate from `docs/platform-compliance.md`'s existing EN/ES social-media rotation (different audience, different reason, social posts only) — whether the *website* switcher should also include Spanish is still open, pending Alan's answer. | 2026-08-03 |
| Docs nightly backup — approach | N8N cloud workflow (not a local scheduled task or GitHub Actions), sourcing files from GitHub (`QueenAxiom/-goose-`, `main` branch — not local disk, which n8n cloud can't read) and fanning out nightly to a Google Drive folder (dated snapshots) plus a summary email to rise@empoweredathome.com. Consulted automation-engineer, website-manager, and operations-manager before building; automation-engineer's rationale: existing GitHub/Drive/Gmail credentials are already connected in n8n, so this needed no new integration surface and stays consistent with N8N as the canonical automation tool. Build status: importable JSON + design doc exist (`automation/docs-nightly-backup.json`, `docs/docs-nightly-backup-workflow.md`), not yet imported into the live N8N instance. | 2026-08-05 |
| Docs nightly backup — approach SUPERSEDED | Supersedes the N8N/GitHub row above. Alan pointed out the local Google Drive Streaming client already works on this machine (`C:\Users\DELL\Google Drive Streaming\My Drive`) — anything written there syncs to Drive automatically, no N8N/GitHub-push dependency needed. Switched to a local mechanism instead: `automation/backup-docs-to-drive.ps1` copies the same 5 docs into a dated subfolder (`Axiom Backups\YYYY-MM-DD\`) inside that synced folder, run daily at 8am (changed from an initial 1am same day) via a Windows Scheduled Task (`AxiomDocsBackupToDrive`, registered 2026-08-05). This is simpler and has no dependency on docs being pushed to GitHub first — it backs up whatever's on disk right now, committed or not. The N8N/GitHub JSON (`automation/docs-nightly-backup.json`) is left in the repo as a reference design, not the active mechanism. Trade-off: this backup only runs if the machine is on and logged in at 1am, unlike a cloud-hosted N8N workflow. | 2026-08-05 |
| WordPress Pro import — sequencing | HELD pending Alan's input, not started. Council flagged a conflict: migrating the three brand sites to WordPress.com Pro now, then restructuring them for the locked EN/FR bilingual rebuild (2026-08-03 entry above) right after, means redoing site architecture twice. Recommendation (not yet confirmed by Alan): bundle the platform migration into the bilingual rebuild as one combined project rather than two separate disruptive touches. Open questions before any work starts: confirm "WordPress Pro" means WordPress.com's Pro hosting plan; confirm each site's current host; confirm whether migration should be bundled with or done before the bilingual rebuild; confirm if there's deadline pressure. | 2026-08-05 |
| Council structure | Council-of-five (Strategist, Builder, Analyst, Guardian, Voice — mandates were to live in a `council-of-five-briefs.md` that was never actually created) is DELETED. Replaced by gstack's plan-review skill suite (`/plan-ceo-review`, `/plan-eng-review`, `/plan-design-review`, `/plan-devex-review`, `/autoplan`, `/codex`), already routed in this repo's `CLAUDE.md`. The informal "council" (ad hoc pull-in of relevant specialist agents before a cross-cutting decision, as used elsewhere in this log) is unaffected — this only removes the named five-role structure and its unbuilt briefs file. Guardian continues to exist as its own standing agent, unrelated to the deleted five-role structure. Sophia/"Queen Axiom" orchestrator concept remains open, now decoupled from council-of-five. See `content-automation-pipeline.md`. | 2026-08-06 |

| Website ecosystem rebuild — stack & domain architecture | LOCKED. Stack: Next.js 15 (App Router) + TypeScript + Tailwind, one monorepo (`apps/empoweredathome`, `apps/everythinginternet`, `apps/schmucksdebate`, `apps/alangooding` + shared `packages/ui, i18n, cms, commerce`), Sanity CMS, Stripe for commerce, Vercel hosting, `next-intl` for EN/FR routing. Domain architecture: **four separate domains, not subdomains** — the earlier outside advice to consolidate everything under one domain with subdomains is rejected; subdomains would pay the SEO-fragmentation cost of separate domains and the brand-dilution cost of consolidation without cleanly getting either benefit, and a founder-name parent domain reads smaller, not bigger, to the B2B buyer the ecosystem needs to convince. alangooding.ca's role is a founder/authority hub that links out to the three brands, not a parent that absorbs them. Consistency across the four sites comes from the shared monorepo/design system, not from merging domains — this keeps a future subdirectory consolidation (the technically-correct version of "one site," if ever wanted) a routing change, not a rebuild. Full rationale: `docs/website-ecosystem-rebuild-plan.md`. Council-produced (architect + marketing-director + media-studio), confirmed by Alan. | 2026-08-06 |
| WordPress Pro import — sequencing SUPERSEDED (second time) | Supersedes both the 2026-08-05 N8N/GitHub row and its 2026-08-05 local-scheduled-task supersession — this entry is about site *hosting*, not the docs-backup automation, which stays on its already-locked local mechanism. Alan's full ecosystem rebuild (see row above) supersedes any WordPress.com Pro hosting migration: migrating the two healthy revenue sites (EmpoweredAtHome, EverythingInternet) to WordPress Pro now, only to rebuild them on Next.js within the year, is pure downside. Open question not yet answered by Alan: whether a WordPress Pro plan was already purchased/committed — if so, it's usable only as a temporary bilingual holding page for schmucksdebate.com (currently down) while the real rebuild is underway, nothing more. | 2026-08-06 |
| Website ecosystem rebuild — timeline | Alan confirmed a roughly one-year build-out horizon, not the aggressive ~17-week phase schedule the architect agent proposed. SchmucksDebate.com still goes first (it's already down, so there's no migration risk — see `docs/website-ecosystem-rebuild-plan.md` sequencing section), but subsequent phases (EverythingInternet.ca, EmpoweredAtHome.com, alangooding.ca) are not gated on a tight weekly cadence. Open factual items (WordPress Pro purchase status, whether a SchmucksDebate content backup exists, whether EverythingInternet's $499 listing checkout currently completes) are treated as first-phase investigation work, not pre-build blockers. | 2026-08-06 |
| Website ecosystem rebuild — EXECUTION START confirmed | Alan initially asked for "a new design for everythinginternet.ca," then clarified the real ask is broader: current site sprawl is disorganized and costing time, so all four sites (SchmucksDebate.com, EverythingInternet.ca, EmpoweredAtHome.com, alangooding.ca) should actually be migrated onto the already-locked plan (Next.js 15 monorepo, Sanity, Stripe, Vercel — see 2026-08-06 stack/domain row) rather than left as a plan on paper. Alan confirmed sequencing stays as locked — one site at a time, not parallel builds — explicitly rejecting a parallel-build option to avoid destabilizing the two healthy revenue sites (EmpoweredAtHome, EverythingInternet) before the shared platform is proven on the low-risk SchmucksDebate.com pilot. This entry marks the move from "plan locked" to "execution starts," beginning with `docs/website-ecosystem-rebuild-plan.md`'s Phase 0 investigation items. | 2026-08-08 |

| Website ecosystem rebuild — PAUSED, EverythingInternet.ca Monday push takes priority | Alan narrowed scope again: skip the full multi-site rebuild for now, focus everything on making the *current* EverythingInternet.ca WordPress/WooCommerce site actually close $499 sales starting Monday 2026-08-10. Live checkout audit (browser walkthrough) found the real blocker: checkout only offers "Direct bank transfer" — no Stripe/PayPal/card gateway — meaning a cold B2B buyer has to wire money to an unfamiliar business before anything happens. That's the actual revenue blocker, not a broken page. The Next.js/Sanity/Stripe monorepo rebuild plan (2026-08-06) stays locked as the longer-horizon plan but work on it is paused. | 2026-08-08 |
| EverythingInternet.ca — Stripe card checkout confirmed live; business model clarified as marketplace | Supersedes the checkout-blocker part of the row above: Alan connected the WooCommerce Stripe Gateway and confirmed live in-admin that "Stripe Optimized Checkout is now active" — card payment is no longer bank-transfer-only, clearing the Monday 2026-08-10 revenue blocker. Separately, Alan confirmed EverythingInternet.ca should support **automatic vendor onboarding** (vendors self-register and connect their own Stripe account via Split Pay's Stripe Connect flow, no manual per-vendor setup) — this answers the "still open" question in `docs/website-ecosystem-rebuild-plan.md` (#4: lead-gen directory vs. e-commerce storefront). It's a multi-vendor marketplace, not a pure lead-gen directory. Mechanically this needs a Stripe **platform secret key** (full permissions, not the restricted/OAuth key WooCommerce's gateway auto-populated) pasted into Split Pay's own settings before vendor Stripe-account connections will work — flagged as an open item in `docs/credential-inventory.md`. Alan confirmed he holds Split Pay licenses usable across all three brand sites (EmpoweredAtHome, EverythingInternet, SchmucksDebate), not just this one. | 2026-08-08 |

| Split Pay vendor onboarding — PAUSED, no further spend | Live checkout for the $499 listing (Stripe via WooPayments) is confirmed separately working and unaffected by this. Split Pay's platform secret key was successfully connected (Stripe account "Axiom Enterprises," `acct_1TWIL7DnEBYWZSoB" — readiness check passed) and GoDaddy Payments was deactivated (GoDaddy support advised only one payment gateway should be active). But "Vendor Onboarding" (self-service vendor Stripe Connect sign-up) is Pro-gated; Alan's ~$129 CAD purchase closely matches Split Pay's real 3-site Pro price ($139 USD/year), so the license likely already covers it, but activating it (Freemius account/license page inside WordPress, or via users.freemius.com) wasn't completed and Alan declined to spend more time or money on it right now. Not a blocker for the Monday 2026-08-10 revenue goal — resume whenever there's an actual vendor needing onboarding. | 2026-08-08 |
| Prospecting automation — discovered, archived, retired | An undocumented scheduled automation ran ~4x/day from 2026-07-23 to 2026-08-09 (52 cycles), researching partnership/affiliate/vendor candidates for all three brand sites and routing every output as a self-addressed Gmail draft (`[Axiom Prospecting] <date>`, sent only to rise@empoweredathome.com) — never to any external recipient, and never logged in `automation/prospecting-log.md` despite that file existing for exactly this purpose. Zero outreach was ever sent in any of the 52 cycles. Two structural blockers (the `docs/platform-compliance.md` product-photo requirement, and `sales-manager`'s charter not covering vendor/affiliate/partnership-BD work) were flagged 10+ times each without resolution. Discovered and archived 2026-08-09; full history preserved at `docs/company-history/prospecting-automation-2026-07-23-to-2026-08-09.md`. **This automation is retired and must not be resumed or referenced as active by future work.** Superseded by manual/agent-assisted outreach going forward — see `automation/prospecting-log.md` for future entries, which should actually get logged this time. | 2026-08-09 |

| WordPress Pro plan — purchase confirmed, migration instructed | Answers the open question in the 2026-08-06/2026-08-08 WordPress Pro rows above ("was Pro already purchased?"). Alan confirmed: Axiom previously held 2 Managed WordPress plans + 1 Website Builder plan; these have been upgraded to a single WordPress.com Pro plan supporting up to 5 sites. Alan wants the brand domains migrated onto this consolidated plan. This supersedes the prior "hold WordPress Pro migration, it's pure downside before the Next.js rebuild" council recommendation (2026-08-06/08 rows) — Alan is instructing the migration proceed. Domain-to-old-plan mapping and migration order not yet confirmed. | 2026-08-10 |
| Content post language rotation — expanded to 8 languages; Guardian gains inspection + email-draft duties | Supersedes the EN/ES-only rotation set in `content-agent.md` and the discrepancy flagged in `docs/platform-compliance.md`. Alan requested a broader language set: posts now cycle one language per post through English, Spanish, French, Arabic, Ukrainian, Chinese, Japanese, Filipino (8 posts/day, one full cycle). Rather than build a new standalone "inspector" agent, extended the existing Guardian agent (already the documented quality gate) to run a Post Inspection Checklist — language correctly labeled/next-in-rotation, destination platform labeled, image sized to a new default Platform Specs table (Instagram/Facebook/X/blog — no confirmed specs existed before, filled with industry-standard defaults pending Alan override) — and to create a Gmail **draft** (not live send) to `rise@empoweredathome.com` for confirmation once a post passes, per the already-locked Facebook/Threads Guardian-review-then-Gmail-draft pattern (2026-08-02 row). Guardian's tools list now includes Gmail draft creation. Updated: `docs/platform-compliance.md`, `.claude/agents/guardian.md`, `.claude/agents/content-agent.md`, `automation/content-log.md`. | 2026-09-04 |
| Content Approval Pipeline — external council rounds added, no new integration | Extends the same-day language/Guardian decision above. Alan wants each post to go through external review/improvement rounds between his interim and final approval — but explicitly does NOT want a paid API integration to another AI vendor; it's a manual relay: Guardian (or whichever agent) prepares a copy-paste-ready package (post text + a review prompt), Alan pastes it into whatever external platform he chooses, pastes the response back, and the agent incorporates the edits. Full flow: Content Agent drafts → Guardian interim check + Gmail draft → Alan's interim confirmation → external council round (manual relay) → incorporate → Guardian final check (full re-check, since council edits can break compliance) + final Gmail draft → Alan's final confirmation and manual publish. No agent auto-publishes at any point, consistent with the locked no-zero-touch-bypass decision (2026-08-02 row). | 2026-09-04 |
| Content Approval Pipeline — council round count is variable, agent must ask after every round | Supersedes the fixed "two rounds" shape in the row directly above (same day, same feature — round count was assumed too early). Alan corrected: he wants to be able to take one opinion, two, six, however many he wants — the agent must never assume a round count. After incorporating every single external council round (including the very first), the agent is required to explicitly ask Alan "do you want to stop here, or get another opinion?" before proceeding to Guardian's final check. Only Alan's answer decides whether another round happens. Updated: `docs/platform-compliance.md` (Content Approval Pipeline section rewritten as a mandatory ask-after-each-round loop, not a fixed 2-round sequence), `.claude/agents/guardian.md`, `automation/content-log.md` (status vocabulary changed to `in council review (round N)`). | 2026-09-04 |
| WordPress Pro migration — execution attempt PAUSED, network change needed | Alan instructed starting with everythinginternet.ca and empoweredathome.com, accepting migration-failure risk ("if it doesn't work we rebuild"). Live GoDaddy migration wizard walkthrough got as far as the Connection Key step for everythinginternet.ca (Manual installation path — ManageWP Worker plugin confirmed already active on the site, connection key located via GoDaddy Quick Links → Connection Management) before Alan paused: "migration out of the question till i change networks." Nothing was migrated/cut over. Resume once Alan is on a different network — pick back up at pasting the connection key into the wizard. | 2026-08-10 |
| WordPress Pro — plan location clarified; approach changed from migrate to fresh build | Resuming 2026-08-10's paused work (network had changed) surfaced that "WordPress.com Pro (5 sites)" is not an Automattic wordpress.com plan — the wordpress.com account tied to Alan's login has zero active purchases. The real 5-site plan is GoDaddy's own **"Pro 5" Managed WordPress hosting tier**, already active and currently anchored to schmucksdebate.com (1 of 5 site slots used). The wordpress.com migration wizard (using the everythinginternet.ca connection key from the prior session) would have required *buying a brand-new* WordPress.com plan (Personal/Premium/Business/Commerce, Commerce needed for WooCommerce at C$59/mo) — this was flagged to Alan rather than purchased. Alan then changed direction: skip migrating the live everythinginternet.ca site's content entirely — "just build all new sites" and restore products from what's documented, not from the old site. New site created directly under the existing GoDaddy Pro 5 capacity (no purchase), 2 of 5 slots now used. | 2026-08-12 |
| EverythingInternet.ca staging rebuild — WooCommerce + Lifetime Directory Listing product live | First concrete build step on the new Pro-5 staging site (temp domain `1246975.us8.myftpupload.com`, not yet pointed at the live everythinginternet.ca domain): WooCommerce installed/activated (onboarding wizard skipped to avoid entering business details), and the one product confirmed in `docs/products-services.md` — "Lifetime Directory Listing," regular price $2,999, sale price $499, marked Virtual — was created and published. Scope was deliberately capped here per Alan's confirmation: everythinginternet.ca only (not empoweredathome.com/schmucksdebate.com/alangooding.ca yet), and only the one documented product — vendor onboarding (Split Pay/Stripe Connect), directory categories, and site design remain undocumented and were not invented. | 2026-08-12 |

## TODO(Alan)
- This log was empty when created — no decisions had been recorded anywhere in the repo. Add entries here as decisions get made, or ask Recorder to log them after calls/meetings.
