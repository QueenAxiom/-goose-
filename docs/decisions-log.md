# Decisions Log — Axiom Enterprises

Referenced by `operations-manager.md` and `recorder.md` (global agent configs) as the locked source of truth for past decisions. Entries here are **locked**: once a row is added, it is not silently reprioritized around, reversed, or re-debated — only Alan can supersede a decision, and doing so should add a new row noting what changed and why, rather than editing the old one.

| Decision | Choice | Date |
|---|---|---|
| Runway API integration timing | Deferred — not until 2026-07-29 at the earliest (video automation not a priority yet) | 2026-07-24 |
| Messaging/telephony provider | RingCentral (existing paid account) instead of Twilio — no need to pay for/provision a second phone number when RingCentral's API covers SMS/voice | 2026-07-24 |
| RingCentral subscription workflow alert channel | Gmail draft (matches existing `content-log.md`/`prospecting-log.md` automation-notification pattern) over Slack or self-SMS | 2026-07-24 |
| YouTube scheduled auto-upload — scope vs. Runway deferral | Built now, not deferred. The 2026-07-24 Runway deferral is about AI video *generation*; this workflow only *publishes* videos Alan already created — different scope. Confirmed with Alan before building. | 2026-07-26 |
| YouTube scheduled auto-upload — default privacy status | `public` (true zero-touch publish), unlike every other content automation in this repo which drafts and waits for Alan to post manually | 2026-07-26 |
| LinkedIn Company Page scheduled auto-post — build status | Deliberately NOT imported into the live N8N instance and no working credential created. Verified live on Alan's LinkedIn Developer app that Company Page posting (`w_organization_social` / Community Management API) is blocked: the app already has "Share on LinkedIn" (personal-only, `w_member_social`) added, and LinkedIn will not let a second product be requested on the same app — a brand-new, single-product app plus a Page-verification/app-review process is required. Importable JSON + design doc exist at `automation/linkedin-scheduled-post.json` / `docs/linkedin-scheduled-post-workflow.md` for whenever access is unblocked. | 2026-07-27 |
| Threads scheduled auto-post — build status | BLOCKED. Verified on the same Meta app that the Threads API use case is added but not fully customized (no green checkmark on the app's own Dashboard checklist, unlike Pages API); `threads_basic` is "Ready for testing" but `threads_content_publish` (the actual posting permission) is not added. Threads runs on its own separate App ID (`1047046641402198`) and App Secret, distinct from the Facebook App ID/Secret already supplied — the secret was not retrieved. n8n has no dedicated Threads node (confirmed live in the node picker), so any build would need the generic HTTP Request node against `graph.threads.net`. Nothing was created or connected live in n8n. Importable JSON + design doc exist at `automation/threads-scheduled-post.json` / `docs/threads-scheduled-post-workflow.md` for whenever access is unblocked. | 2026-07-27 |
| Facebook/Threads scheduled auto-post — Guardian bypass | OPEN QUESTION, not decided: whether Facebook Page posts and Threads posts should bypass Guardian's Mandatory Post Elements checklist (`docs/platform-compliance.md`) for full zero-touch publish like YouTube, or still route through Guardian review before being marked ready to post. Same unresolved question first flagged on the LinkedIn build (2026-07-27) — still open across all three social builds. | 2026-07-27 |
| Facebook Page scheduled auto-post — build status | MOSTLY VIABLE, connected live but not fully activated. `automation/facebook-scheduled-post.json` is imported into the live N8N instance as its own workflow ("Facebook Scheduled Post"), with every node's credential auto-matched to existing account-level credentials (Facebook Graph account / Google Sheets account / Gmail account). `pages_manage_posts` and `pages_read_engagement` were added in the Meta console and are "Ready for testing". Alan completed the OAuth "Connect" login himself in n8n. Executing "Get Page Access Token" live showed the connected account manages exactly one Facebook Page: **Everythinginternet.ca**, Page ID `1096244530243584` — not a separate "Axiom Enterprises" Page as originally assumed. That ID is hardcoded into the "Extract Target Page Token" node in both `automation/facebook-scheduled-post.json` and the live n8n workflow. Only remaining blocker: creating the "Posts" Google Sheet and supplying its ID. See `docs/facebook-scheduled-post-workflow.md`'s 2026-07-28 follow-up pass. | 2026-07-28 |
| Facebook/Threads scheduled auto-post — Guardian bypass RESOLVED | Supersedes the 2026-07-27 open question. Alan decided: no zero-touch bypass. Guardian reviews the drafted post first, then it's sent to Alan's email for his approval before anything posts — same pattern as every other content automation in this repo (e.g. Pocket AI recorder posts), not the YouTube-style full auto-publish. This reverses the "full zero-touch auto-publish" design basis stated at the top of `docs/facebook-scheduled-post-workflow.md` — that workflow's direct-to-Facebook-API posting nodes need to be replaced with a Guardian-check + Gmail-draft-for-approval step. | 2026-08-02 |
| Website rebuild — bilingual EN/FR requirement | LOCKED, not optional. Quebec's Bill 96 (Charter of the French Language) applies extraterritorially: any company selling to or marketing toward Quebec consumers online must offer a full French version of the site (homepage, product/service descriptions, pricing, policies) with French not subordinate to English (not smaller, not harder to find, not an afterthought). None of Axiom's three sites geo-block Quebec, so this is a live compliance exposure, not a style preference — Quebec's language office has been actively fining companies over English-only pages ($3,000–$30,000 per violation). All three site rebuilds must ship full EN/FR parity with a language selector at the top of the page that swaps the entire site on selection. This is separate from `docs/platform-compliance.md`'s existing EN/ES social-media rotation (different audience, different reason, social posts only) — whether the *website* switcher should also include Spanish is still open, pending Alan's answer. | 2026-08-03 |
| Docs nightly backup — approach | N8N cloud workflow (not a local scheduled task or GitHub Actions), sourcing files from GitHub (`QueenAxiom/-goose-`, `main` branch — not local disk, which n8n cloud can't read) and fanning out nightly to a Google Drive folder (dated snapshots) plus a summary email to rise@empoweredathome.com. Consulted automation-engineer, website-manager, and operations-manager before building; automation-engineer's rationale: existing GitHub/Drive/Gmail credentials are already connected in n8n, so this needed no new integration surface and stays consistent with N8N as the canonical automation tool. Build status: importable JSON + design doc exist (`automation/docs-nightly-backup.json`, `docs/docs-nightly-backup-workflow.md`), not yet imported into the live N8N instance. | 2026-08-05 |
| Docs nightly backup — approach SUPERSEDED | Supersedes the N8N/GitHub row above. Alan pointed out the local Google Drive Streaming client already works on this machine (`C:\Users\DELL\Google Drive Streaming\My Drive`) — anything written there syncs to Drive automatically, no N8N/GitHub-push dependency needed. Switched to a local mechanism instead: `automation/backup-docs-to-drive.ps1` copies the same 5 docs into a dated subfolder (`Axiom Backups\YYYY-MM-DD\`) inside that synced folder, run daily at 8am (changed from an initial 1am same day) via a Windows Scheduled Task (`AxiomDocsBackupToDrive`, registered 2026-08-05). This is simpler and has no dependency on docs being pushed to GitHub first — it backs up whatever's on disk right now, committed or not. The N8N/GitHub JSON (`automation/docs-nightly-backup.json`) is left in the repo as a reference design, not the active mechanism. Trade-off: this backup only runs if the machine is on and logged in at 1am, unlike a cloud-hosted N8N workflow. | 2026-08-05 |
| WordPress Pro import — sequencing | HELD pending Alan's input, not started. Council flagged a conflict: migrating the three brand sites to WordPress.com Pro now, then restructuring them for the locked EN/FR bilingual rebuild (2026-08-03 entry above) right after, means redoing site architecture twice. Recommendation (not yet confirmed by Alan): bundle the platform migration into the bilingual rebuild as one combined project rather than two separate disruptive touches. Open questions before any work starts: confirm "WordPress Pro" means WordPress.com's Pro hosting plan; confirm each site's current host; confirm whether migration should be bundled with or done before the bilingual rebuild; confirm if there's deadline pressure. | 2026-08-05 |
| Council structure | Council-of-five (Strategist, Builder, Analyst, Guardian, Voice — mandates were to live in a `council-of-five-briefs.md` that was never actually created) is DELETED. Replaced by gstack's plan-review skill suite (`/plan-ceo-review`, `/plan-eng-review`, `/plan-design-review`, `/plan-devex-review`, `/autoplan`, `/codex`), already routed in this repo's `CLAUDE.md`. The informal "council" (ad hoc pull-in of relevant specialist agents before a cross-cutting decision, as used elsewhere in this log) is unaffected — this only removes the named five-role structure and its unbuilt briefs file. Guardian continues to exist as its own standing agent, unrelated to the deleted five-role structure. Sophia/"Queen Axiom" orchestrator concept remains open, now decoupled from council-of-five. See `content-automation-pipeline.md`. | 2026-08-06 |

## TODO(Alan)
- This log was empty when created — no decisions had been recorded anywhere in the repo. Add entries here as decisions get made, or ask Recorder to log them after calls/meetings.
