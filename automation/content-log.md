# Content Draft Log

One row per scheduled content-drafting run. `status`: `drafted` (Gmail draft created, awaiting Alan) / `rejected` (Guardian blocked it — see notes) / `posted` (Alan confirmed it went live — update manually).

Language alternates EN → ES → EN ... in row order. Next run reads the last row's `language` and flips it.

| date | site | language | status | notes |
|---|---|---|---|---|
